<?php

/**
 * Con las rutas post los parametros se reciven normal como siempre
 * se procesa y al final puedes renderizar redireccionar o devolver un json al cliente
 * 
 * $_POST["Parametro post"];
 * Response::redirect($ruta);
 * Response::render($archivo);
 * Response::sendJson($PHPObject);
 */
